import { Box } from "@chakra-ui/react";

// the HomePage will be our default page after log in
export const Home = () => {
    return (
        <Box>
            <p>Home Page</p>
        </Box>
    );
};